query BidirectionalCharactersForClass($id: String, $offset: Int = 0, $limit: Int = 100) {
  unicodeObject(type: bidiclass, id: $id) {
    chars(offset: $offset, limit: $limit) {
      id
      text
    }
  }
}